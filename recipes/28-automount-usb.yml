architecture: {{ .architecture }}

actions:
  - action: apt
    description: Install Autofs
    packages:
      - autofs

  - action: overlay
    description: Automount USB Config
    source: ../overlays/28-automount-usb
    destination: /
