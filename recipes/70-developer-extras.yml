{{- $architecture := or .architecture "arm64" -}}

architecture: {{ $architecture }}

actions:
  - action: apt
    description: Install Extra Development Packages
    packages:
      - less
      - iputils-ping
      - htop

  - action: run
    description: Add ssl-cert group
    chroot: true
    command: |
      groupadd ssl-cert