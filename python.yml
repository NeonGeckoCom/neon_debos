{{- $architecture := or .architecture "arm64" -}}
{{- $build_cores := or .build_cores 4 -}}
{{ $suite :=  or .suite "bookworm" }}


architecture: {{ $architecture }}
actions:
  - action: recipe
    recipe: recipes/00-build-python-3_10.yml
    variables:
      architecture: {{ $architecture }}
      build_cores: {{ $build_cores }}
      suite: {{ $suite }}